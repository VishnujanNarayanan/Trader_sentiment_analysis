# Streamlit Community Cloud installs from this file. Deliberately narrower than
# requirements-dev.txt: the deployed app needs neither the notebook stack nor
# the test tooling.
streamlit>=1.30
pandas==2.2.3
scipy>=1.10
scikit-learn>=1.3
scikit-posthocs>=0.9
